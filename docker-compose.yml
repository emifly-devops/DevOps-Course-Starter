version: "3"

services:
  production:
    build:
      context: .
      target: production
    image: ${DOCKERHUB_USERNAME}/todo-app:latest
    environment:
      MONGO_URI: ${MONGO_URI}
    ports:
      - "8080:80"
